# AGENTS_DOCS_ENV_KO.md

이 문서는 개발 작업 중 사후 문서화(README 작성 등) 및 로컬 개발 환경(가상 환경 관리)에 관련된 지침을 담고 있습니다. 에이전트 구동 시 토큰 절약을 위해 상시 주입 규칙(`AGENTS_KO.md`)에서 분리되었습니다.

---

## 1. README 및 설명 작성 규칙

- **동어반복 금지**: README 문서나 CLI 옵션 도움말을 작성할 때, 단어의 정의를 단순히 반복하는 무의미한 문장 작성을 금지한다.
  - *잘못된 예: `--dialect: 사용할 dialect입니다.`*
  - *올바른 예: `--dialect: 생성된 쿼리를 실행할 대상 데이터베이스 엔진 및 문법 규격 (예: duckdb, postgres)입니다.`*
- **세부 설명 제공**: 사용자가 도구의 목적, 입력 파라미터의 비즈니스적 도메인 의미, 그리고 설정값들이 시스템 전체에 미치는 영향 범위를 온전히 이해할 수 있도록, 핵심 용어 정의, 상세 작동 원리 및 구체적인 활용 예시를 포함하여 가독성 있게 작성한다.

---

## 2. 가상 환경 분리 및 워크스페이스 관리 규칙

- **서브 프로젝트 가상 환경 격리**: 각 하위 프로젝트 디렉터리(예: `llm_api`, `sql_to_dbt_yml_api` 등) 내부에 구성된 독립된 가상 환경은 해당 서브 프로젝트 전용으로만 사용한다. 해당 프로젝트의 핵심 기능과 무관한 패키지를 추가 설치하여 가상 환경을 오염시키지 않도록 철저히 격리하여 관리한다.
- **워크스페이스 구성**: 멀티루트 워크스페이스 구조로 하위 프로젝트가 다수 존재하는 경우, 원활한 관리를 위해 워크스페이스 루트 디렉터리에 `{워크스페이스 이름}.code-workspace` 설정 파일을 생성하여 프로젝트 간 전환과 구성을 명확히 관리한다.

