# AGENTS_KO.md

## 코딩 규칙

- **하드코딩 금지**: 
  1. 설정값, 파일 경로, 모델명, API URL, 인증 정보, 비즈니스 상수, 레이블 텍스트, 사용자용 경고(Warning) 및 에러 메시지 등의 데이터는 소스 코드 내에 직접 작성하지 않는다. 대신 설정 파일(예: YAML)이나 환경 변수에서 로드하도록 하여 코드와 데이터를 분리한다. (단, 단순 개발자용 디버깅/추적 로그 포맷은 코드 내에 선언할 수 있다.)
  2. 계정 암호, API 키 등 보안이 필요한 설정값은 환경 변수로 관리한다.
  3. 예외처리 에러메시지, 일정한 패턴이 있는 다량의 데이터, 휴리스틱 규칙(정규식 패턴 매핑, 판별 임계값 등)은 별도의 전용 사전(Dictionary) 형식 설정 파일(예: YAML 등)을 구성하여 분리하고 관리한다.
- **빠른 실패(Fail-Fast) 및 프로그램 안정성**:
  1. 프로그램 실행에 필요한 필수 설정값(Required)은 반드시 설정 파일에 정의되어야 한다. 설정값이 누락되었을 경우 코드 내 임의의 상수로 대체하여 불완전하게 동작시키지 않고, 누락된 사실을 로그 및 CLI를 통해 사용자에게 즉각 알린 후 즉시 실행을 종료(Fail-Fast)하여 빠른 실패를 유도한다. 단, 선택적 설정값(Optional)에 한하여 코드 내에서 기본값으로 `""`(빈 문자열), `[]`(빈 리스트), `None`(null), `0`(숫자 0)과 같은 상수를 사용하는 것은 허용한다.
  2. 설정값 누락으로 인한 프로그램 강제 종료는 오직 프로그램 초기 구동 단계(예: 웹 서버의 Startup 생명주기 이벤트 시점, CLI의 기동 직후 등)에만 발생해야 한다. 구동 완료 후 실제 요청이나 비즈니스 작업 처리 단계에서는 예외 처리를 통해 프로세스가 예기치 않게 종료되지 않도록 방어하고, 비즈니스 로직에 따라 오류를 복구하거나 일관된 에러 응답을 안정적으로 반환해야 한다.
- **객체 지향 설계**: 복잡하거나 중요한 비즈니스 로직을 추가 및 수정할 때는 객체 지향 설계 원칙을 적용한다. 단일 책임 원칙(SRP)에 따라 책임을 명확히 분리한 고응집 클래스를 설계하며, 거대하고 비대해진 절차적 함수 작성을 피한다.
- **공통 모듈 설계**: 프로젝트의 전체 소스 코드 규모가 2,000라인을 초과하는 시점부터는 `common` 패키지(혹은 폴더) 하위에 `logging`, `config`, `error` 처리를 전담하는 독립된 클래스들을 설계하여 모듈 간 공용으로 사용하도록 일원화한다. 특히, 운영 환경에서의 런타임 로그 기록을 위해 `print()` 함수를 사용하는 것을 금지한다.

## 한국어 주석 작성 규칙

- 한글이 포함된 모든 소스 코드 및 문서 파일은 반드시 **UTF-8 인코딩**으로 저장한다.
- 소스 코드 주석 및 Docstring은 한글(한국어)로 작성하는 것을 원칙으로 한다.
- 모든 클래스와 함수에는 그 역할, 목적, 설계 의도를 상세히 설명하는 한글 Docstring을 작성한다.
- 클래스 변수에는 해당 변수의 도메인적 의미, 유효한 값의 허용 범위, 그리고 후속 데이터 흐름에서의 역할 등을 명확히 설명하는 주석을 추가한다. 단순히 변수명이나 타입을 직역하여 반복하는 무의미한 주석 작성을 금지한다.

## Python 파일 헤더 규칙

- 모든 Python 파일(`.py`)의 최상단에는 파일 생성일, 설계자(작성자) 이름, 소속, 연락 가능한 이메일 주소를 주석으로 명시한다.
- 이 헤더 주석은 모듈 Docstring, `import` 구문, 기타 실행 코드보다 가장 먼저 배치되어야 한다.
- 헤더 주석 바로 아래에는 해당 파일의 목적, 역할, 주요 기능을 설명하는 모듈 수준의 한글 Docstring을 작성한다.
- 헤더 주석 및 모듈 Docstring은 아래 지정된 템플릿 형식을 준수하여 작성한다.

```python
# 작성일: YYYY-MM-DD
# 설계자: 이름
# 설계자 소속: 회사명
# 설계자 이메일: name@example_corp.com, name@example_personnel.com

"""
이 파일의 목적과 주요 기능에 대한 상세한 한글 설명입니다.
"""
```


## 로그 작성 규칙

- **단일행 출력**: 외부 로그 수집 및 통합 시스템(예: Logstash, Fluentd, AWS CloudWatch 등)을 통한 자동 파싱과 처리를 원활하게 하기 위해, 모든 로그 레코드는 반드시 **단일 행(Single Line)**으로 기록되어야 한다.
- **개행 문자 처리**: 로그 메시지나 직렬화된 데이터(예: SQL 쿼리, LLM 응답 본문, 예외 스택 트레이스 등) 내에 개행 문자(`\n`, `\r`)가 포함되는 경우, 이를 공백(Space)으로 치환하거나 적절하게 이스케이프 처리하여 로그 행이 끊어지지 않도록 플랫(Flat)하게 변환한다.
- **맥락 보존**: 디버깅과 장애 추적이 용이하도록, 입력값(예: 요청 파라미터, 자연어 쿼리)과 그에 따른 실행 결과(생성된 SQL, 경고 목록 등)를 동일한 로그 행 안에서 함께 확인할 수 있도록 상관관계(Correlation) 정보를 함께 기록한다.

## 가상 환경 활용 규칙

- **루트 가상 환경 활용 (공통/일반 작업)**: 특정 서브 프로젝트에 종속되지 않는 일반 스크립트 실행, 패키지 추가 설치(pip install) 등의 유틸리티 작업 시에는 워크스페이스 루트 폴더의 공통 가상 환경(예: `venv312` 또는 `.venv`)을 사용하여 하위 프로젝트의 전용 가상 환경이 오염되지 않도록 한다.
