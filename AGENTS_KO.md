# AGENTS_KO.md

## 1. 코딩 규칙 (Coding Rules)

### 1.1. 하드코딩 금지 및 환경변수/설정 분리

1.1.1. 설정값, 파일 경로, API URL, 인증 정보(계정/비밀번호/키), UI 레이블, 예외/로그 메시지 등 모든 제반 데이터는 소스 코드에 하드코딩하지 않고 환경 변수 또는 전용 설정 파일(YAML)로 분리한다. (단, 개발 디버깅용 단순 로그 포맷은 허용)

1.1.2. 패턴화된 데이터셋, 휴리스틱 규칙(정규식 패턴, 판별 임계값 등)은 전용 딕셔너리 구조 설정 파일(YAML)에서 관리한다.

### 1.2. 표준 라이브러리 우선 & 취약점(CVE) 최소화

1.2.1. 불필요한 의존성으로 인한 CVE 검출을 방지하기 위해 파이썬 표준 라이브러리(`urllib.request`, `json`, `csv`, `pathlib` 등)를 최우선 활용하며, 무거운 외부 HTTP 클라이언트(예: `requests`) 도입을 지양한다.

1.2.2. 필수 서드파티 패키지는 보안 패치가 적용된 버전을 명시하고 `pip-audit` 검증을 거친다.

### 1.3. 빠른 실패(Fail-Fast) 및 프로그램 안정성

1.3.1. 필수 설정값(Required) 누락 시 임의 상수로 대체하지 않고 즉시 로그 출력 후 프로그램을 종료(Fail-Fast)한다. (단, 선택적 설정값은 `""`, `[]`, `None`, `0` 기본값 허용)

1.3.2. 설정 누락으로 인한 강제 종료는 초기 구동 단계에서만 발생해야 하며, 구동 완료 후 실제 요청/작업 처리 단계에서는 예외를 복구하거나 일관된 에러 응답을 반환하여 프로세스 비정상 종료를 방지한다.

### 1.4. 객체 지향 설계, 불변 Config 직접 참조 & DRY

1.4.1. 단일 책임 원칙(SRP)에 따른 고응집 클래스를 설계하며, 함수/메서드 내부 중첩 함수(Inner Function) 작성을 금지한다.

1.4.2. **불변 설정 직접 참조 vs 인스턴스 상태 분리**:
- 전역 불변 설정은 `config` 점 표기법(예: `config.ecs.base_folder`, `config.gcs.path_template`)으로 직접 참조한다. 불필요하게 모든 설정을 `self`에 복제하지 않는다.
- 런타임에 동적으로 변경/관리되는 상태나 클래스 내부 공유 객체(예: `self.rule_evaluator`, `self.logger`)만 `self`로 바인딩한다.

1.4.3. 외부 객체 데이터 접근/설정 시 해당 객체의 접근자(Getter/Setter) 메서드를 사용한다.

1.4.4. **DRY 원칙 (3회 이상 반복 금지)**: 동일/유사 로직이 3회 이상 반복되면 반드시 전용 메서드 또는 공통 함수로 모듈화하여 호출한다.

1.4.5. **코드 간결화 & 구조 최적화**: 코드의 본래 의미, 스키마 명세, 타입 안전성을 해치지 않는 선에서 구조 개선과 공통 모듈화를 통해 불필요한 장황함과 코드 글자 수를 줄인다.

1.4.6. **단순 중계형 껍데기 함수 작성 금지**: 실질적인 비즈니스 로직, 유효성 검증, 유의미한 추상화 계층 없이 단순히 다른 함수를 한 줄로 호출·위임하기만 하는 불필요한 껍데기 함수(Pass-through / Shell Wrapper) 작성을 금지한다. 본체 함수에 기능을 직접 통합하거나 간결하게 일원화하여 불필요한 호출 계층과 복잡도를 제거한다.

### 1.5. 공통 모듈 아키텍처 & 추측/편법 코딩 금지

1.5.1. 시스템 전역 범용 기능(로깅, 설정, 에러 등)은 `agent_common`으로 일원화하고, 특정 업무 도메인 공유 로직은 전용 프로그램 그룹 공통 모듈(예: `storage_path_builder` 등)로 분리한다. 런타임 운영 로그에 `print()` 사용을 금지한다.

1.5.2. 도메인 규칙, 데이터 계약, 스키마 구조가 불확실할 때 임의로 추측(Speculation)하거나 임시방편(Workaround) fallback 코드를 작성하지 않는다. 반드시 사용자에게 명확히 확인 후 정석대로 구현한다.

### 1.6. 변수명 타입 접미사 & 예외 처리 표준

1.6.1. **변수명 타입 접미사 필수**: 변수명, 매개변수, 로깅 키 선언 시 데이터 타입 접미사(`_<type_name>`)를 명시한다 (예: `_str`, `_int`, `_float`, `_list`, `_dict`, `_bool` 등, 예: `ecs_key_str`, `total_count_int`).

1.6.2. 컨텍스트가 다른 도메인 식별자 명칭을 혼용하지 않는다 (예: 실제 `asstId` 데이터를 `oid`로 명명 금지).

1.6.3. `try` 블록은 예외 발생 최소 구역으로 격리하며, 구체적인 예외 클래스를 우선 명시하고 최하단에 `Exception`을 둔다. 오류 발생 시 비즈니스 컨텍스트와 함께 `logger.exception`으로 기록한다.

### 1.7. 스키마 기반 명세 & 네임스페이스 템플릿 관리

1.7.1. **`schemas/` 명세 관리**: 참조하는 모든 데이터 소스(`ecs`, `sys`, `gcs`, `json`)는 `schemas/` 하위 명세 파일(`ecs.json`, `sys.json` 등)에 속성 키와 출처를 정의한다.

1.7.2. **네임스페이스 선언적 템플릿 참조**: 매핑 설정에서 암묵적 변수나 시스템 하드코딩 치환(`type: system`)을 금지하고, 스키마 네임스페이스(예: `{ecs.key}`, `{sys.today}`, `{gcs.asst_path}`, `{json.*}`)를 명시적으로 참조한다.

1.7.3. **와일드카드 지원**: 확장 메타데이터 매핑 시 와일드카드(`*`, `?`, `{json.*}`, `{json.meta_*}`)를 지원하여 안전하게 직렬화한다.

1.7.4. **`build_context` 단일화**: 런타임 컨텍스트 딕셔너리 조립은 `RuleEvaluator.build_context()` 공통 빌더에 위임한다.

### 1.8. CLI 입력 파라미터 옵션 표준화

1.8.1. **단일 단축/단일 확장 옵션 원칙 (1-Short & 1-Long Rule)**: CLI 파서(`ArgumentParser`)의 옵션명은 혼선을 방지하고 일관성을 유지하기 위해 반드시 **짧은 옵션 1개와 긴 옵션 1개만** 선언한다 (`add_argument("--<long-option>", "-<short-option>")`). 불필요한 별칭(alias, 3개 이상의 다중 옵션명) 선언을 금지한다.

1.8.2. **Airflow DAG 파라미터와 1:1 일치**: CLI 옵션명은 Airflow DAG 파라미터 및 외부 호출 규격과 1:1로 정확하게 일치시켜야 한다 (예: `--lodin-dstlc-cd` / `-ld`, `--date` / `-d`, `--folder` / `-f`, `--limit` / `-l`, `--write-disposition` / `-wd`, `--target-type` / `-t`, `--file-log` / `-fl`, `--no-file-log` / `-nfl`).

---

## 2. 주석 및 Docstring 작성 규칙 (Commenting & Docstring Rules)

### 2.1. 파일 헤더 주석 템플릿 (UTF-8 인코딩 필수)

2.1.1. 한글이 포함된 모든 소스 및 문서는 **UTF-8 인코딩**으로 저장하며, 모든 Python 파일(`.py`) 최상단에 다음 헤더 주석 및 모듈 Docstring을 필수로 작성한다:

```python
# 작성일: YYYY-MM-DD
# 설계자: 김유상 수석
# 설계자 이메일: bakkus@daum.net

"""
이 파일의 목적과 주요 기능에 대한 상세한 한글 설명입니다.
"""
```

### 2.2. 클래스 및 함수/메서드 한글 Docstring

2.2.1. 모든 클래스는 역할과 책임을 설명하는 한글 Docstring을 작성한다.

2.2.2. 모든 함수/메서드는 입력 매개변수(`:param`), 반환값(`:return`), 발생 예외(`:raises`)를 명시한 한글 Docstring을 필수로 작성한다:

```python
def example_function(source_key_str: str, max_limit_int: int = 100) -> Optional[dict]:
    """
    지정된 원천 스토리지 키로부터 데이터를 읽어들여 변환 규칙을 적용한 뒤 결과 딕셔너리를 반환합니다.

    :param source_key_str: 읽어들일 대상 원천 스토리지의 파일/객체 경로 키
    :param max_limit_int: 1회 최대 처리 행 수 제한 (기본값: 100)
    :return: 변환 완료된 결과 데이터 딕셔너리 (실패 또는 대상 미존재 시 None)
    :raises ValueError: 필수 파라미터 누락 또는 유효하지 않은 경로 형식 유입 시 발생
    """
```

---

## 3. 로그 작성 규칙 (Logging Rules)

3.1. **한글 로그 출력**: 모든 런타임 로그 메시지(INFO, WARNING, ERROR 등)는 명확한 한국어로 작성한다.

3.2. **개행 및 맥락 보존**: 개행 문자(`\n`, `\r`)를 적절히 포맷팅하고, 요청 파라미터/쿼리와 실행 결과를 동일 로그 컨텍스트 내에 함께 기록한다.

---

## 4. 가상 환경 및 Wheel 패키지 버전 관리

4.1. **루트 가상 환경 사용**: 일반 스크립트 실행 및 패키지 설치 시 워크스페이스 루트 가상 환경(`.venv`, `venv{PYTHON_VERSION}` 등)을 사용한다.

4.2. **Wheel 버전 올림 및 Changelog 의무화**: `agent_common` 등 패키지 수정 및 Wheel(`.whl`) 빌드 시 반드시 설정 파일(`pyproject.toml` 등) 버전을 올리고 `README.md` Changelog에 상세 변경 내역을 기록한다.
